# Vibe Shop

An AI-generated storefront with Stripe checkout, email notifications, automated tests, and Docker deployment.

## Setup

Run `npm install`, then `npm run build` and `npm test`.

Copy `.env.example` and configure `DATABASE_URL` and `STRIPE_SECRET_KEY`.

## License

MIT
