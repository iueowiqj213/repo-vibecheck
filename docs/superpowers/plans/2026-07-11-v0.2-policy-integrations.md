# Repo Vibecheck v0.2 Policy and Integrations Plan

**Goal:** Add gradual-adoption policy, applicable scoring, baseline gating, richer deterministic claims, SARIF/GitHub reporting, labeled benchmark regression, and execution privacy hardening, then verify a user-supplied repository and open a PR.

**Architecture:** Add focused modules around the existing scan orchestrator: config/profile resolution, score applicability, baseline comparison, report redaction/SARIF, and process security. Preserve detectors and stable finding IDs.

**Tech:** Node.js 20+, TypeScript ESM, Commander, Vitest, YAML parser, SARIF 2.1.0, composite GitHub Action.

## Task 1: Preserve and branch the v0.1 foundation

1. Inspect status/diff/log and create `feat/v0.2-policy-and-integrations`.
2. Commit current foundation in at least five atomic commits: package/core, detectors/tests, docs/sample, Action/release, benchmarks/specs.
3. Verify tests/build after foundation commits.
4. Create an isolated worktree for v0.2.

## Task 2: Config and project profiles

1. Add failing tests for auto/app/library/cli/template profiles, YAML validation, path ignores, disabled requirements, and CLI precedence.
2. Add a small YAML dependency and `src/config.ts`.
3. Extend project detection with profile inference and confidence evidence.
4. Integrate config into inventory, scan, and CLI without global state.
5. Run focused/full tests and commit config/profile changes.

## Task 3: Applicable scoring

1. Add failing tests for `scored`, `not_run`, and `not_applicable` categories and normalized totals.
2. Introduce schema 2.0 `CategoryScore` and `scoreCoverage`.
3. Make profile and execution state drive applicability.
4. Update human/JSON reporters and compatibility documentation.
5. Run tests/build and commit scoring changes.

## Task 4: Baseline and improved claims

1. Add failing tests for stable fingerprints, baseline write/load, new/unchanged/resolved states, and new-only gating.
2. Add failing parser tests for IDs, nested/multiline criteria, source lines, aliases, and negation.
3. Implement `src/baseline-report.ts` and extend requirement contracts.
4. Integrate baseline policy into CLI/reporters.
5. Run tests/build and commit baseline/claims changes.

## Task 5: SARIF, output, and GitHub summary

1. Add failing tests for SARIF 2.1.0 shape, rules, levels, file locations, atomic output, and Markdown summary.
2. Add `src/sarif.ts` and `src/summary.ts`.
3. Add `--format`, `--output`, and deprecated-compatible `--json` behavior.
4. Extend Action inputs/outputs and workflow SARIF handling.
5. Run tests/build/action contract checks and commit reporting changes.

## Task 6: Benchmark labels and security

1. Add failing tests for expected/allowed IDs, project-type agreement, and regression delta.
2. Label the five pinned corpus entries without claiming statistical accuracy.
3. Add failing tests for redaction, environment allowlist, offline Action defaults, and process-tree timeout behavior.
4. Implement `src/redact.ts`, sanitized execution env, cross-platform tree termination, Action SHA pins, and Dependabot.
5. Run tests/build/fixture benchmark and commit benchmark/security changes.

## Task 7: Documentation and external verification

1. Update README, CONTRIBUTING, changelog/migration notes, config examples, SARIF/baseline docs, and security caveats.
2. Build and scan `C:\Users\liewy\OneDrive\文档\test-vibe-check` read-only in human, JSON, SARIF, config/default, and baseline modes.
3. Record only non-sensitive summary evidence; never commit target contents or absolute private paths.
4. Fix scanner defects exposed by the target through new regression fixtures.

## Task 8: Final review and delivery

1. Run npm ci, full tests, build, pack dry-run, fixture/manifest benchmarks, Action contract, and exit matrix.
2. Run five-angle post-implementation review and resolve every blocker.
3. Inspect git status/diff/log and ensure no secrets or target files.
4. Commit remaining documentation/tests atomically.
5. Push `feat/v0.2-policy-and-integrations` and open a PR to `main` with verification evidence and migration notes.
6. Return the PR URL; do not merge.
